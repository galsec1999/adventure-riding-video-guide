# מסירה ל־Codex — אצווה 1

## מקורות אמת

`MASTER_SPEC.md` קובע דרישות; `data/categories.json` הוא מילון הסיווג; `schema/video.schema.json` מגדיר רשומת וידאו; `data/videos.json` מכיל רק תוכן מאושר; `data/learning-paths.json` מפנה רק למזהים מאושרים.

## מבנה הנתונים

ה־ID הפנימי הוא `yt-` בתוספת מזהה YouTube. כל השדות המסווגים משתמשים במזהים מהמילון המבוקר. ערכים שלא אומתו נשארים `null` או מערך ריק. `verification.content_evidence_types` מתעד את בסיס הסיווג.

## השלכות UI

יש להציג הפרדה ברורה בין שטח, כביש, משולב, תרגול ובטיחות. המסננים צריכים להיגזר מקובץ הקטגוריות ולהציג שמות עבריים. יש לאפשר חיפוש באמצעות `data/synonyms.json`, להציג אזהרת בטיחות, ולהציג מקור וקישור YouTube בכל כרטיס.

## אסור לשנות

אין לשנות תקצירים, סיווג, דירוג איכות, ראיית אימות, מזהי וידאו או מסלולי למידה כדי להתאים UI. שינוי תוכן דורש בדיקת מקור ותיעוד. אין ליצור fallback של נתוני דמה.

## בעיות ידועות

האצווה מכסה 60 מתוך יעד מינימלי 200. כיסוי עברי וקטגוריות דרך מסוימות עדיין דלים. זמינות קישורים דורשת בדיקה מחודשת בסבבים הבאים.
